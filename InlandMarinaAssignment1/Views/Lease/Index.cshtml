@model IEnumerable<InlandData.Lease>

@{
    ViewData["Title"] = "My Leases";
}
<div class="d-flex justify-content-center">
    <div class="table-responsive table-striped col-6 justify-content-center">
        <h1>My Leases</h1>
        @if (TempData["Message"] != null)
        {
            <div class="alert alert-success">
                @TempData["Message"]
            </div>
        }
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                   @*   <th>
                         @Html.DisplayNameFor(model => model.LeaseId)
                     </th> *@
                    <th>
                        @Html.DisplayNameFor(model => model.SlipID)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Slip.Length)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Slip.Width)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Slip.Dock.Name)
                    </th>

                  @*   <th>
                        @Html.DisplayNameFor(model => model.CustomerID)
                    </th> *@
                    <th></th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model) {
                <tr>
                  @*   <td>
                        @Html.DisplayFor(modelItem => item.LeaseId)
                    </td> *@
                    <td>
                        @Html.DisplayFor(modelItem => item.SlipID)
                    </td>
                     <td>
                        @Html.DisplayFor(modelItem => item.Slip.Length)
                    </td>
                     <td>
                        @Html.DisplayFor(modelItem => item.Slip.Width)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Slip.Dock.Name)
                    </td>
                  @*   <td>
                        @Html.DisplayFor(modelItem => item.CustomerID)
                    </td> *@
                 @*    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td> *@
                </tr>

        }
    </tbody>
</table>

</div>
</div>
